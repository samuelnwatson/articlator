<div class="container">
  <div class="col-sm-4">
    <%= link_to "new article", new_article_path %>
  </div>

  <div class="col-sm-4">
    <div>Articles: </div>
    <ul>
      <% @articles.each do |article| %>
        <li>Title: <%= article.title %></li>
      <% end %>
    </ul>
  </div>
</div>
